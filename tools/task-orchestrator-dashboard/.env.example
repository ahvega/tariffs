# Task Orchestrator Dashboard Configuration

# Path to the task-orchestrator SQLite database
# Default: data/tasks.db (relative to dashboard directory)

# =============================================================================
# DOCKER INSTALLATION (MCP Docker Toolkit)
# =============================================================================
# If using task-orchestrator via Docker (docker-compose.yml), the database
# is stored in a named volume: mcp-task-data
# Container path: /app/data/tasks.db
#
# To find the volume path on your host machine:
#
#   docker volume inspect mcp-task-data
#   # Look for "Mountpoint" in the output
#
# Common Docker volume paths:
#
# Linux/macOS:
#   /var/lib/docker/volumes/mcp-task-data/_data/tasks.db
#
# Windows (WSL2 backend):
#   \\wsl$\docker-desktop-data\data\docker\volumes\mcp-task-data\_data\tasks.db
#   or
#   \\wsl.localhost\docker-desktop-data\data\docker\volumes\mcp-task-data\_data\tasks.db
#
# Windows (Docker Desktop with direct access):
#   %LOCALAPPDATA%\Docker\wsl\data\ext4.vhdx (database is inside this virtual disk)
#
# Alternative: Copy database from Docker container:
#   docker compose cp mcp-task-orchestrator:/app/data/tasks.db ./data/tasks.db
#   # Then use: TASK_ORCHESTRATOR_DB=data/tasks.db
#
# =============================================================================
# STANDALONE/LOCAL INSTALLATION
# =============================================================================
#
# Relative path (if tasks.db is in dashboard folder):
#   TASK_ORCHESTRATOR_DB=data/tasks.db
#
# Absolute path examples:
#   macOS/Linux: /home/user/task-orchestrator/data/tasks.db
#   Windows: C:\Users\YourName\task-orchestrator\data\tasks.db
#
# =============================================================================

# Default configuration (update based on your setup)
TASK_ORCHESTRATOR_DB=data/tasks.db

# Example for Docker (uncomment and update path):
# TASK_ORCHESTRATOR_DB=/var/lib/docker/volumes/mcp-task-data/_data/tasks.db

# Example for Windows WSL2 Docker (uncomment and update):
# TASK_ORCHESTRATOR_DB=\\wsl$\docker-desktop-data\data\docker\volumes\mcp-task-data\_data\tasks.db
